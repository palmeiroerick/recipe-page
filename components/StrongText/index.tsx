import GenericChildren from "@/types/GenericChildren";

const StrongText = ({ children }: GenericChildren) => {
  return <span className="font-bold text-wengeBrown">{children}</span>;
};

export default StrongText;
